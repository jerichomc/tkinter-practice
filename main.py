from tkinter import *

window = Tk() #instantiate an instance of a window
window.geometry("640x640") #height and width of window
window.title("practice tkinter")

window.mainloop() # will show the window on screen and listen for events
